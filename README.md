# alpine based cassandra cqlsh

a handy docker image to run cassandra cqlsh.

# alias

add following alias anywhere you need to run a docker container as default cqlsh.

`alias cqlsh='docker run --rm -it loivis/cqlsh`
